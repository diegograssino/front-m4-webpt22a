const RegisterHeader = () => {
  return (
    <header className="h-16 bg-secondary m-4 rounded-lg flex items-center justify-center">
      Register
    </header>
  );
};

export default RegisterHeader;
